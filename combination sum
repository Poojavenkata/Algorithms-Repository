def combination_sum(candidates, target):
    a = [0] * (target + 1)
    a[0] = 1
    for i in candidates:
        for j in range(i, len(a)):
            a[j] = a[j] + a[j - i]

    return a[len(a) - 1]
